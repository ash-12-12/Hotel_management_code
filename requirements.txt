langchain==0.0.1  # Specify the exact version if needed
langchain_community==0.0.1  # Specify the exact version if needed
langchain_core==0.0.1  # Specify the exact version if needed
langchain_google_genai==0.0.1  # Specify the exact version if needed
langgraph==0.0.1  # Specify the exact version if needed
pydantic==1.8.2  # Specify the exact version if needed
sentence-transformers==2.2.0  # Specify the exact version if needed
chromadb==0.3.1  # Specify the exact version if needed
sqlite3  # SQLite is typically built-in with Python, so no need to install separately
